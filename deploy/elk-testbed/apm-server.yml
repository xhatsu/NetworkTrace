apm-server:
  host: "0.0.0.0:8200"
  rum:
    enabled: true
  kibana:
    enabled: false

output.elasticsearch:
  hosts: ["http://elasticsearch:9200"]

queue:
  mem:
    events: 256
    flush.min_events: 64
    flush.timeout: 1s

logging.level: info
